<?xml version="1.0" encoding="utf-8"?>
<!-- App Admin (super-admin) : simple tableau de bord web. Elle n'a besoin QUE d'internet.
     On retire ici toutes les permissions du taximètre, sinon Android les
     réclamerait à l'installation (« accéder à votre position », « s'afficher
     par-dessus les autres apps ») pour rien — et ça inquiète l'utilisateur. -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" tools:node="remove" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" tools:node="remove" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" tools:node="remove" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" tools:node="remove" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" tools:node="remove" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" tools:node="remove" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" tools:node="remove" />

    <application>
        <service android:name=".TaximeterService" tools:node="remove" />
    </application>
</manifest>
